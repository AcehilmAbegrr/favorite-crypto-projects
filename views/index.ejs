<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>MY FAVORITE CRYPTO PROJECTS</title>
</head>
<body>
    These are some great blockchain projects. Check them out for yourself!
    <ul class="projects">
        <% for(let i=0; i<projects.length; i++) {%>
        <li class="project">
            <span><%= projects[i].project %></span>
            <span><a href=<%- projects[i].site %>><%- projects[i].site %></a></span>
        </li>
        <% } %>
    </ul>
    <form action="/crypto-projects" method="POST">
        <input type="text" placeholder="project name" name="project">
        <input type="url" placeholder="site" name="site">
        <button type="submit">Submit</button>
    </form>
</body>
</html>